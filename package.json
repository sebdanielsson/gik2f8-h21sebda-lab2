{
    "name": "gik2f8-lab2",
    "version": "1.0.0",
    "description": "Simple to-do app with backend and frontend.",
    "scripts": {
        "start": "node app.mjs",
        "dev": "nodemon app.mjs",
        "test": "echo \"Error: no test specified\" && exit 1",
        "tailwind-dev": "npx tailwindcss -o ./build/output.css --watch",
        "build": "npx tailwindcss -o ./build/output.css;mkdir -p ./build; ls -1 ./src/ | grep -v ^input.css | grep -v ^.DS_Store | xargs -I{} mv src/{} ./build;cp README.md ./build/README.md"
    },
    "author": {
        "name": "Sebastian Danielsson",
        "email": "sebastian.danielsson@proton.me",
        "url": "https://github.com/SebDanielsson"
    },
    "license": "MIT",
    "dependencies": {
        "express": "4.18.2"
    },
    "devDependencies": {
        "nodemon": "2.0.21",
        "tailwindcss": "3.2.7"
    },
    "engines": {
        "node": "18.14.2",
        "npm": "9.6.0"
    }
}
